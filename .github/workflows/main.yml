name: CI

on:
  push:
    branches: [ teste-deploy ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Installing dependencies
        run: npm i

      # - name: Deploying to Heroku
      #   run: git push heroku teste-deploy:main
